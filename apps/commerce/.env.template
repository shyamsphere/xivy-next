# ── Core ─────────────────────────────────────────────────────────────
DATABASE_URL=postgres://xivy:xivy@localhost:5433/xivy_commerce
REDIS_URL=redis://localhost:6380
JWT_SECRET=supersecret
COOKIE_SECRET=supersecret
MEDUSA_BACKEND_URL=http://localhost:9000
# shared (default) | server | worker — split on Railway
MEDUSA_WORKER_MODE=shared

# ── CORS (storefront :3000, admin :9000/:5173) ───────────────────────
STORE_CORS=http://localhost:3000
ADMIN_CORS=http://localhost:5173,http://localhost:9000
AUTH_CORS=http://localhost:3000,http://localhost:5173,http://localhost:9000

# ── File storage: Cloudflare R2 via S3 API (optional locally) ────────
# S3_FILE_URL=https://cdn.xivy.in
# S3_BUCKET=xivy-media
# S3_REGION=auto
# S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# S3_ACCESS_KEY_ID=
# S3_SECRET_ACCESS_KEY=

# ── Google OAuth for customers (optional locally) ────────────────────
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_CALLBACK_URL=http://localhost:3000/auth/google/callback

# ── Razorpay (Phase 4) ────────────────────────────────────────────────
# RAZORPAY_KEY_ID=
# RAZORPAY_KEY_SECRET=
# RAZORPAY_WEBHOOK_SECRET=

# ── Shiprocket (Phase 5) ──────────────────────────────────────────────
# SHIPROCKET_EMAIL=
# SHIPROCKET_PASSWORD=
# SHIPROCKET_PICKUP_LOCATION=Primary
# SHIPROCKET_WEBHOOK_TOKEN=

# ── Resend (Phase 4) ──────────────────────────────────────────────────
# RESEND_API_KEY=
# RESEND_FROM=Xivy <orders@xivy.in>
